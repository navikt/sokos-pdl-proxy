query($ident: ID!) {
    hentIdenter(ident: $ident, historikk: true, grupper: [FOLKEREGISTERIDENT, NPID]) {
        identer {
            ident,
            historisk,
            gruppe
        }
    }
}