query($ident: ID!) {
    hentIdenter(ident: $ident, historikk: true, grupper: [FOLKEREGISTERIDENT, NPID, AKTORID]) {
        identer {
            ident,
            historisk,
            gruppe
        }
    }
}